# 视觉反馈效果（Juice）实现计划

> **For agentic workers:** Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add 5 visual feedback effects to increase game "juice" — hit-stop, kill burst, low health vignette, boss entrance, and boss phase transition.

**Architecture:** Leverage existing infrastructure (TimeManager for slow-mo, Renderer for screen effects, Camera for shake). Add hit-stop to TimeManager, enhance particle spawning in game.js, add low-health vignette to Renderer, and wire boss effects through existing event system.

**Tech Stack:** HTML5 Canvas, vanilla JavaScript

---

## File Structure

```
pixel-dungeon/
├── js/
│   ├── game.js          # Modify: TimeManager + hit-stop + kill burst + boss entrance
│   ├── boss.js          # Modify: phase transition effects
│   ├── renderer.js      # Modify: low health vignette rendering
│   ├── constants.js     # Modify: add FEEDBACK config for new effects
│   └── camera.js        # No changes (already has shake)
```

---

## Task 1: 命中停顿（Hit-stop / Freeze Frames）

**Files:**
- Modify: `pixel-dungeon/js/game.js` (TimeManager class, ~line 108)
- Modify: `pixel-dungeon/js/game.js` (collision handling, ~line 1880)
- Modify: `pixel-dungeon/js/constants.js` (FEEDBACK config)

- [ ] **Step 1: 在constants.js的FEEDBACK中添加hit-stop配置**

在 `FEEDBACK.SLOW_MOTION` 之后添加：

```javascript
// 命中停顿（冻结帧）
HIT_STOP: {
    ENEMY_HIT: 30,      // 击中普通敌人冻结30ms
    ENEMY_KILL: 50,     // 击杀敌人冻结50ms
    BOSS_HIT: 40,       // 击中Boss冻结40ms
    CRIT_HIT: 60        // 暴击冻结60ms
}
```

- [ ] **Step 2: 在TimeManager类中添加freezeFrame系统**

在 `TimeManager` 构造函数中添加：

```javascript
// 冻结帧
this.freezeTimer = 0;
this.isFrozen = false;
```

在 `TimeManager` 类中添加方法：

```javascript
/**
 * 触发冻结帧（命中停顿）
 * @param {number} duration - 冻结持续时间（毫秒）
 */
freeze(duration) {
    this.freezeTimer = Math.max(this.freezeTimer, duration);
    this.isFrozen = true;
}

/**
 * 获取缩放后的delta time（含冻结帧）
 * @param {number} originalDelta - 原始delta time
 * @returns {number}
 */
getScaledDeltaTime(originalDelta) {
    if (this.isFrozen && this.freezeTimer > 0) {
        return 0; // 冻结期间返回0，游戏逻辑停止更新
    }
    return originalDelta * this.timeScale;
}
```

修改 `TimeManager.update()` 方法，在开头添加冻结帧更新：

```javascript
update(deltaTime) {
    // 更新冻结帧（使用真实时间）
    if (this.freezeTimer > 0) {
        this.freezeTimer -= deltaTime;
        if (this.freezeTimer <= 0) {
            this.freezeTimer = 0;
            this.isFrozen = false;
        }
    }
    
    // 更新时间停止（原有代码）
    // ...
```

- [ ] **Step 3: 在碰撞检测中触发hit-stop**

找到 `game.js` 中子弹击中敌人的碰撞处理代码（`checkCollisions` 方法中），在敌人受伤后添加：

```javascript
// 击中停顿
this.timeManager.freeze(FEEDBACK.HIT_STOP.ENEMY_HIT);
```

找到击杀敌人的位置（`onEnemyKilledEvent` 或碰撞检测中 `enemy.health <= 0` 时），添加：

```javascript
// 击杀停顿（更强）
this.timeManager.freeze(FEEDBACK.HIT_STOP.ENEMY_KILL);
```

找到Boss被击中的位置（`boss.takeDamage` 或类似），添加：

```javascript
// Boss击中停顿
this.timeManager.freeze(FEEDBACK.HIT_STOP.BOSS_HIT);
```

- [ ] **Step 4: 验证hit-stop效果**

测试：
- 射击普通敌人 → 画面轻微停顿30ms
- 击杀敌人 → 画面停顿50ms，更明显
- 射击Boss → 画面停顿40ms

- [ ] **Step 5: 提交**

```bash
git add js/game.js js/constants.js
git commit -m "feat: add hit-stop freeze frames on enemy hit/kill/boss hit"
```

---

## Task 2: 击杀爆发增强

**Files:**
- Modify: `pixel-dungeon/js/game.js` (spawnDeathParticles method, ~line 3458)
- Modify: `pixel-dungeon/js/constants.js` (PARTICLES config)

- [ ] **Step 1: 在constants.js中添加击杀爆发配置**

在 `PARTICLES.TYPES` 中添加：

```javascript
KILL_BURST: {
    // 径向爆射粒子
    RADIAL_COUNT: 30,
    RADIAL_SPEED_MIN: 4,
    RADIAL_SPEED_MAX: 10,
    RADIAL_SIZE_MIN: 3,
    RADIAL_SIZE_MAX: 7,
    RADIAL_LIFETIME: 500,
    // 环形冲击波
    RING_COUNT: 16,
    RING_SPEED: 8,
    RING_SIZE: 6,
    RING_LIFETIME: 300,
    // 中心闪光
    FLASH_SIZE: 40,
    FLASH_LIFETIME: 200
}
```

- [ ] **Step 2: 增强spawnDeathParticles方法**

替换 `game.js` 中的 `spawnDeathParticles` 方法：

```javascript
spawnDeathParticles(x, y, color, isCritKill = false) {
    const cfg = PARTICLES.TYPES.KILL_BURST;
    const mult = isCritKill ? 1.5 : 1.0; // 暴击击杀1.5倍粒子
    
    // 径向爆射粒子（主要视觉）
    const radialCount = Math.floor(cfg.RADIAL_COUNT * mult);
    for (let i = 0; i < radialCount; i++) {
        const angle = (i / radialCount) * Math.PI * 2 + Math.random() * 0.2;
        const speed = cfg.RADIAL_SPEED_MIN + Math.random() * (cfg.RADIAL_SPEED_MAX - cfg.RADIAL_SPEED_MIN);
        const size = cfg.RADIAL_SIZE_MIN + Math.random() * (cfg.RADIAL_SIZE_MAX - cfg.RADIAL_SIZE_MIN);
        
        const particle = new Particle(
            x, y,
            Math.cos(angle) * speed,
            Math.sin(angle) * speed,
            Math.random() < 0.3 ? '#ffffff' : color,
            size,
            cfg.RADIAL_LIFETIME
        );
        particle.setKind(PARTICLES.KIND.CIRCLE);
        particle.setGravity(0.1);
        this.particles.push(particle);
    }
    
    // 环形冲击波（快速扩散）
    for (let i = 0; i < cfg.RING_COUNT; i++) {
        const angle = (i / cfg.RING_COUNT) * Math.PI * 2;
        const particle = new Particle(
            x, y,
            Math.cos(angle) * cfg.RING_SPEED,
            Math.sin(angle) * cfg.RING_SPEED,
            color,
            cfg.RING_SIZE,
            cfg.RING_LIFETIME
        );
        particle.setKind(PARTICLES.KIND.CIRCLE);
        particle.setGravity(0);
        this.particles.push(particle);
    }
    
    // 中心白色闪光
    const flash = new Particle(
        x, y, 0, 0,
        '#ffffff',
        cfg.FLASH_SIZE * mult,
        cfg.FLASH_LIFETIME
    );
    flash.setKind(PARTICLES.KIND.CIRCLE);
    flash.setGravity(0);
    this.particles.push(flash);
    
    // 小碎片（额外细节）
    for (let i = 0; i < 15; i++) {
        const angle = Math.random() * Math.PI * 2;
        const speed = 1 + Math.random() * 3;
        const particle = new Particle(
            x, y,
            Math.cos(angle) * speed,
            Math.sin(angle) * speed - 2,
            '#ffffff',
            1 + Math.random() * 2,
            300 + Math.random() * 200
        );
        particle.setKind(PARTICLES.KIND.SQUARE);
        particle.setGravity(0.15);
        this.particles.push(particle);
    }
}
```

- [ ] **Step 3: 在击杀事件中触发屏幕闪光**

在 `onEnemyKilledEvent` 中添加：

```javascript
// 击杀屏幕闪光
renderer.triggerScreenFlash(color, 0.15);
```

- [ ] **Step 4: 验证击杀效果**

测试：击杀敌人时应看到径向粒子爆射 + 中心白色闪光 + 环形冲击波 + 屏幕轻微闪色。

- [ ] **Step 5: 提交**

```bash
git add js/game.js js/constants.js
git commit -m "feat: enhance kill burst with radial particles, ring shockwave, and screen flash"
```

---

## Task 3: 低血量暗角

**Files:**
- Modify: `pixel-dungeon/js/renderer.js` (Renderer constructor + renderScreenEffects)
- Modify: `pixel-dungeon/js/game.js` (update loop)

- [ ] **Step 1: 在Renderer构造函数中添加低血量暗角属性**

在 Renderer 构造函数中，`this.screenFlashColor` 之后添加：

```javascript
// 低血量红色暗角
this.lowHealthVignetteAlpha = 0;
this.lowHealthVignettePulse = 0;
```

- [ ] **Step 2: 添加低血量暗角更新方法**

在 Renderer 类中添加：

```javascript
/**
 * 更新低血量暗角效果
 * @param {number} healthPercent - 玩家血量百分比（0-1）
 * @param {number} deltaTime - 时间增量
 */
updateLowHealthVignette(healthPercent, deltaTime) {
    if (healthPercent <= 0.3) {
        // 低于30%血量，暗角逐渐增强
        const targetAlpha = (0.3 - healthPercent) / 0.3 * 0.6; // 最大0.6
        this.lowHealthVignetteAlpha += (targetAlpha - this.lowHealthVignetteAlpha) * 0.05;
        
        // 脉动效果（心跳感）
        this.lowHealthVignettePulse += deltaTime * 0.005;
        const pulse = Math.sin(this.lowHealthVignettePulse) * 0.15;
        this.lowHealthVignetteAlpha = Math.max(0, this.lowHealthVignetteAlpha + pulse);
    } else {
        // 高于30%血量，暗角淡出
        this.lowHealthVignetteAlpha *= 0.95;
        if (this.lowHealthVignetteAlpha < 0.01) {
            this.lowHealthVignetteAlpha = 0;
        }
    }
}
```

- [ ] **Step 3: 在renderScreenEffects中渲染低血量暗角**

在 `renderScreenEffects` 方法中，在"受伤红边"渲染代码之前添加：

```javascript
// 低血量红色暗角（带脉动）
if (this.lowHealthVignetteAlpha > 0.01) {
    const gradient = ctx.createRadialGradient(
        this.width / 2, this.height / 2, Math.min(this.width, this.height) * 0.2,
        this.width / 2, this.height / 2, Math.max(this.width, this.height) * 0.7
    );
    gradient.addColorStop(0, 'rgba(0, 0, 0, 0)');
    gradient.addColorStop(1, `rgba(180, 0, 0, ${this.lowHealthVignetteAlpha})`);
    ctx.fillStyle = gradient;
    ctx.fillRect(0, 0, this.width, this.height);
}
```

- [ ] **Step 4: 在game.js的update循环中调用**

在 `game.js` 的 `update` 方法中，在 `uiManager.update` 之后添加：

```javascript
// 更新低血量暗角
if (this.player) {
    const healthPercent = this.player.health / this.player.maxHealth;
    renderer.updateLowHealthVignette(healthPercent, scaledDelta);
}
```

- [ ] **Step 5: 验证低血量暗角**

测试：让玩家受伤到低血量（<30%），屏幕边缘应出现红色暗角并有心跳般的脉动。血量恢复后暗角淡出。

- [ ] **Step 6: 提交**

```bash
git add js/renderer.js js/game.js
git commit -m "feat: add low health red vignette with heartbeat pulse effect"
```

---

## Task 4: Boss出场动画

**Files:**
- Modify: `pixel-dungeon/js/game.js` (onBossSpawnEvent, ~line 2404)

- [ ] **Step 1: 增强Boss出场事件处理**

替换 `game.js` 中的 `onBossSpawnEvent` 方法：

```javascript
onBossSpawnEvent(data) {
    const { boss } = data;
    if (boss) {
        this.boss = boss;
    }
    
    // Boss出场动画序列
    this.bossEntranceAnimation();
}

/**
 * Boss出场动画
 * 阶段1: 镜头拉近 (0-500ms)
 * 阶段2: 屏幕震动+闪白 (500-1000ms)
 * 阶段3: 慢动作恢复 (1000-1500ms)
 */
bossEntranceAnimation() {
    // 音效
    audioManager.playSound(AUDIO.BOSS_APPEAR);
    
    // 阶段1: 镜头拉近
    renderer.setCameraZoom(1.3);
    
    // 500ms后: 屏幕震动+闪白
    setTimeout(() => {
        camera.shake(8, 500);
        renderer.triggerScreenFlash('#ff0000', 0.4);
    }, 500);
    
    // 1000ms后: 慢动作
    setTimeout(() => {
        this.timeManager.startSlowMotion(500, 0.3);
    }, 1000);
    
    // 1500ms后: 恢复正常镜头
    setTimeout(() => {
        renderer.resetCameraZoom();
    }, 1500);
}
```

- [ ] **Step 2: 验证Boss出场动画**

测试：进入Boss房间时，应看到镜头拉近 → 屏幕震动+红色闪白 → 慢动作 → 镜头恢复。

- [ ] **Step 3: 提交**

```bash
git add js/game.js
git commit -m "feat: add boss entrance animation with camera zoom, shake, flash, and slow-mo"
```

---

## Task 5: Boss阶段切换特效

**Files:**
- Modify: `pixel-dungeon/js/boss.js` (startPhaseTransition method, ~line 259)

- [ ] **Step 1: 在startPhaseTransition中添加屏幕特效**

在 `boss.js` 的 `startPhaseTransition` 方法中，在 `particleSystem.createPhaseTransition` 之后添加：

```javascript
// 屏幕闪白
if (typeof renderer !== 'undefined' && renderer.triggerScreenFlash) {
    const flashColor = newPhase === 2 ? '#ff6600' : '#ffff00';
    renderer.triggerScreenFlash(flashColor, 0.5);
}

// 相机震动
if (typeof camera !== 'undefined' && camera.shake) {
    camera.shake(6, 400);
}

// 短暂慢动作
if (typeof game !== 'undefined' && game.timeManager && game.timeManager.startSlowMotion) {
    game.timeManager.startSlowMotion(300, 0.2);
}
```

- [ ] **Step 2: 验证Boss阶段切换**

测试：将Boss打到50%血量触发阶段2 → 屏幕闪橙色+震动+慢动作。打到25%触发阶段3 → 屏幕闪黄色+震动+慢动作。

- [ ] **Step 3: 提交**

```bash
git add js/boss.js
git commit -m "feat: add boss phase transition screen flash, shake, and slow-mo effects"
```

---

## Task 6: 集成测试

**Files:**
- No new files, testing only

- [ ] **Step 1: 测试命中停顿**

1. 进入战斗房间
2. 射击敌人
3. 验证：击中时画面有30ms停顿
4. 击杀敌人时有50ms停顿
5. 射击Boss时有40ms停顿

- [ ] **Step 2: 测试击杀爆发**

1. 击杀敌人
2. 验证：径向粒子爆射 + 环形冲击波 + 中心白色闪光 + 屏幕轻微闪色

- [ ] **Step 3: 测试低血量暗角**

1. 让玩家受伤到<30%血量
2. 验证：屏幕边缘红色暗角 + 心跳脉动
3. 恢复血量
4. 验证：暗角淡出

- [ ] **Step 4: 测试Boss出场**

1. 进入Boss房间
2. 验证：镜头拉近 → 屏幕震动+红色闪白 → 慢动作 → 镜头恢复

- [ ] **Step 5: 测试Boss阶段切换**

1. 将Boss打到50%血量
2. 验证：屏幕闪橙色+震动+慢动作
3. 打到25%
4. 验证：屏幕闪黄色+震动+慢动作

- [ ] **Step 6: 性能测试**

1. 在粒子密集时检查FPS是否保持60fps
2. 验证冻结帧不会导致卡死

- [ ] **Step 7: 最终提交**

```bash
git add -A
git commit -m "feat: complete visual feedback (juice) system

- Hit-stop freeze frames on enemy hit/kill/boss hit
- Enhanced kill burst with radial particles, ring shockwave, screen flash
- Low health red vignette with heartbeat pulse
- Boss entrance animation (camera zoom, shake, flash, slow-mo)
- Boss phase transition effects (screen flash, shake, slow-mo)"
```
